{
  "name": "videoreview",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "npx esbuild src/sw-main.js --bundle --format=esm --outfile=background.js --platform=browser && npx esbuild src/auth-page.js --bundle --outfile=auth-page.js --format=iife --platform=browser",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sadakab/Notch.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Sadakab/Notch/issues"
  },
  "homepage": "https://github.com/Sadakab/Notch#readme",
  "dependencies": {
    "@supabase/supabase-js": "^2.100.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
